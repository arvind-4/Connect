services:
  app:
    build:
      context: .
      dockerfile: app.Dockerfile
    container_name: app
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: /usr/src/connect/commands/docker-start.sh
