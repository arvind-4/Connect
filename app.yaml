services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django-app
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    command: /usr/src/connect/.venv/bin/daphne backend.asgi:application --bind 0.0.0.0 --port 8000
